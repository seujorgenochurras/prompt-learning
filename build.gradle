plugins {
    id 'java'
    id "com.diffplug.spotless" version "7.0.0.BETA4"

}
repositories {
    mavenCentral()
}
dependencies {

}
subprojects {
    apply plugin: "com.diffplug.spotless"

    spotless {
        java {
            eclipse()
                    .configFile("${rootDir}/.formatter/Default.xml")
        }
    }
}
test {
    useJUnitPlatform()
}
